<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.energize">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Energize">
        <activity android:name=".ThemeSolar"></activity>
        <activity android:name=".ResultScreen"></activity>
        <activity android:name=".SelectAvatar"/>
        <activity android:name=".ThemeSelection" />
        <activity android:name=".LanguageSelection" />
        <activity android:name=".AccountDetails" />
        <activity android:name=".SelectAvatar_Dialog"
            android:screenOrientation="portrait"
            android:theme="@style/popupTheme" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>